# Python dependencies validated for Ubuntu 24.04 + Python 3.12 target.
# Install with: python3 -m pip install -r requirements-jazzy.txt

# Core numeric and vision stack
numpy>=1.26,<3
opencv-python>=4.10
scipy>=1.11
pillow>=10.3
pyyaml>=6.0.1
matplotlib>=3.8
tqdm>=4.66
PyQt5==5.15.10
pyserial>=3.5
docker>=7.1
vosk>=0.3.45
sounddevice>=0.4.7

# ML runtimes
# YOLOv5 legacy code in this repo was updated for PyTorch 2.x APIs.
torch>=2.3
torchvision>=0.18

# MediaPipe legacy `solutions` API required by the detector scripts.
mediapipe==0.10.14

# TensorFlow for Python 3.12
tensorflow>=2.17

# Note on GPU/CPU execution:
# Running on CPU (e.g. Intel Core Ultra / x86_64 CPU mode without NVIDIA CUDA GPU).
# Standard PyTorch and TensorFlow wheels will run in CPU mode via OneDNN/MKL.
